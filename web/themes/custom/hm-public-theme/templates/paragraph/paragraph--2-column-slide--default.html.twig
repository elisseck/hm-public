{#
/**
 * @file
 * Theme override for custom block 2 Column Slide.
 *
 * Available variables using Paragraphs:
 * -  field_slide_image
 * -  field_slide_header
 * -  field_slide_content
 *
 */
#}
{# <ol>
  <li>Image:         {{ content.field_slide_image|field_value }}</li>
  <li>Alt Tag:       {{ content.field_slide_image|field_raw('alt') }}</li>
  <li>Slide Header:  {{ content.field_slide_header|field_value }}</li>
  <li>Slide Content: {{ content.field_slide_content|field_value }}</li>
</ol> #}

<!-- Slide -->
<div class="row two-column-slide">
  <div class="col-sm-8 two-column-slide--image" style="background-image: url('{{ file_url(content.field_slide_image|field_target_entity.uri.value) }}');" alt="{{ content.field_slide_image|field_raw('alt') }}">
  </div>
  <div class="col-sm-4 two-column-slide--content">
    <h2>{{ content.field_slide_header|field_value }}</h2>
    <p>{{ content.field_slide_content|field_value }}</p>
  </div>
</div>
