{#
/**
 * @file
 * Theme override for People view and custom blocks.
 *
 * Content Type created using Paragraphs
 * -  Paragraph Label: People
 * -  Paragraph Field: field_accordion_people
 *
 * Available Fields
 * -  field_bio
 * -  field_location
 * -  field_name
 * -  field_picture
 * -  field_position_s
 *
 */
#}

{# {{ drupal_dump(_context|keys) }} #}
{# {{ drupal_dump(_context.rows) }} #}

<div class="accordion">
    <button class="accordion__panel" type="button" data-toggle="collapse" data-target="#{{ view.element['#name']}}{{ view.element['#display_id'] }}" aria-expanded="true" aria-controls="{{ view.element['#name']}}{{ view.element['#display_id'] }}">
          {# Header goes here... #}
        <span class="accordion__arrow">
            <span class="accordion__arrow-half"></span>
            <span class="accordion__arrow-half"></span>
        </span>
    </button>

    <div class="collapse show accordion__body" id="{{ view.element['#name']}}{{ view.element['#display_id'] }}">
        <div class="row col-sm">
          {{ _context.rows }}
        </div>
    </div>
</div>

{#  
<div class="card--list col-sm-6">
  <div class="card--image">
    <img src={{ file_url(content.field_picture|field_target_entity.uri.value) }} alt={{ content.field_picture|field_raw('alt') }} /> 
  </div>
  <div class="card--content">
    <h3>{{ content.field_name|field_value }}</h3>
    <h4>{{ content.field_position_s|field_value }}</h4>
    <h4>{{ content.field_location|field_value }}</h4>
    <p>Read Bio</p>
  </div>
</div> #}
